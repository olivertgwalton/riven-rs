[package]
name = "apalis-redis"
version = "1.0.0-rc.7"
authors = ["Njuguna Mureithi <mureithinjuguna@gmail.com>"]
edition = "2024"
repository = "https://github.com/apalis-dev/apalis-redis"
license = "MIT"
description = "Background task processing for rust using apalis and redis (patched)"
include = ["src/**", "lua/**", "Cargo.toml"]

[dependencies]
apalis-codec = "0.1.0-rc.7"
apalis-core = { version = "1.0.0-rc.7", default-features = false, features = [
    "sleep",
] }
redis = { version = "1.0.4", default-features = false, features = [
    "script",
    "aio",
    "connection-manager",
] }
serde = "1"
log = "0.4.21"
chrono = { version = "0.4.38", default-features = false, features = [
    "clock",
    "serde",
] }
futures = "0.3.30"
tokio = { version = "1", features = ["rt", "net"], optional = true }
smol = { version = "2", optional = true }
thiserror = "2.0.0"
event-listener = "5.4.0"
tower = { version = "0.5" }
ulid = { version = "1.2.1", features = ["serde"] }
serde_json = "1"

[features]
default = ["tokio-comp"]
smol-comp = ["smol", "redis/smol-comp"]
tokio-comp = ["tokio", "tokio/net", "redis/tokio-comp"]

[lib]
name = "apalis_redis"
path = "src/lib.rs"
